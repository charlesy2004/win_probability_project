name: Capture NBA snapshots

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  capture:
    runs-on: ubuntu-latest

    steps:
      - name: Call snapshot endpoint
        run: |
          echo "Calling snapshot endpoint..."
          curl -i -X POST "${{ secrets.SNAPSHOT_ENDPOINT }}"